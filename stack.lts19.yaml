resolver: lts-19.7
packages:
flags:
  nejla-common:
    new-singletons: true

extra-deps:
  - hspec-wai-json-0.11.0

extra-package-dbs: []

nix:
  packages:
    - zlib
    - postgresql

docker:
  enable: false
  image: nejla/haskell-build
